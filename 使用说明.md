# L2D Config Editor 使用说明

## 1. 推荐工作流

1. 首次启动时选择保存 JSON 配置的工作目录；工具会记住该目录。之后可通过“文件 → 更改工作区…”切换。
2. 点击“创建配置底座”，在每一行填写角色名、角色资源名、角色 ID、版本、作者和允许目光拖拽的待机；作者可留空。
3. 需要一次创建多名角色时，在表格里添加多行；也可以从 Excel 复制后点击“粘贴多行”。版本、作者和目光拖拽待机可随角色分别变化。
4. 工具会按版本建立目录，为每行生成一份 JSON，并打开第一份配置。
5. 从画布上的 `idle0（默认待机）` 输出点开始，创建并连接后续交互。
6. 完成后按 `Ctrl+S` 保存；需要检查最终表格时使用 CSV 预览或导出。若当前目录属于 SVN 工作副本，也可点击“提交当前 JSON 到 SVN”。

角色元数据保存在 JSON 顶层的隐式 `meta` 中，不再显示为常驻的“初始节点”。旧版配置会在打开时自动迁移，原 Initial 的位置和出边会保留给 idle0。

## 2. 节点类型

- `idle0（默认待机）`：每份配置唯一的根节点，不可复制、删除或作为连线目标。
- `TouchIdle`：点击或待机切换类交互。
- `TouchDrag`：拖拽或数值变化类交互。
- `参数专用节点`：以表格形式批量维护参数触发行。
- `返回默认待机`：用法与 TouchIdle 相近，但目标待机固定为 idle0，无需填写。
- `备注节点`：只显示一个多行大标题；双击标题直接编辑，`Ctrl+Enter` 或失焦提交，不参与 CSV 输出。

双击功能节点可在摘要卡片和完整字段表单之间切换。旧的全局“简易/高级”入口已经移除；如以后需要不同字段层级，应在节点自身实现。

## 3. 画布操作

- 滚轮：缩放画布（具体修饰键可在快捷键设置中调整）。
- 鼠标中键拖动：平移画布。
- 右键空白处：创建节点。
- 从输出点拖到输入点：建立连接。
- 从输出点拖到空白处：创建新节点并自动连接。
- `Ctrl+左键`：在空白处直接画线；从节点按下并拖动超过 4 个视口像素后画线，
  未超过阈值则保持节点追加/反选。
- 按住 `Ctrl+右键` 滑动：扫过区域命中的笔迹会即时整条隐藏，松开后作为一次
  批量删除提交；快速滑动会按前后鼠标位置形成连续扫掠区域，不会只检查离散点。
- `Esc`：取消正在创建的连接或笔迹，或关闭弹出式搜索框。
- `Delete`：删除选中的节点、连线或参考图。
- `F`：定位当前节点。
- `Ctrl+L`：优化已连接节点的布局。

所有正式连接均使用贝塞尔曲线。选中节点或连线后，方向箭头、脉冲和尾迹会沿曲线运动；大量节点下会自动降低动效密度，但不会退化成直线。

## 4. 计划模式

工具栏可在“正式图 / 计划图”之间一键切换。两个视图共用同一份节点和正式连线；
计划图只保存自己的标题、主父级、同级顺序、折叠、分支颜色与视角，不会改动
正式节点坐标或 CSV 字段。

- `Enter`：新建同级主题；`Tab`：新建子主题；`Shift+Tab`：提升一级。
- `F2` 或双击：编辑计划标题。已有正式节点的标题写入独立 `plan_title`。
- `Space`：折叠/展开；方向键：在可见主题间导航。
- 拖动主题可重排或改父级。改父只替换对应的主正式边，额外引用边保持不变。
- 删除父主题前会显示子树节点数量；确认后整棵子树作为一次操作删除并可一次撤销。
  `idle0` 根主题不可删除或改父。
- 多父、环或额外正式边以虚线引用显示；孤立节点进入“未连接”分支，往返切换
  不会丢失任何正式边。
- 计划图也支持同样的 `Ctrl+左键` 绘制、`Ctrl+右键` 滑动整线删除和一次撤销；
  计划图笔迹与正式图笔迹独立保存，切换或正式化节点时不会互相复制或清空。
- 新建计划主题先生成内部虚节点。标题按大小写不敏感规则填写为
  `touchidle<左编号>-touch_idle<右编号>`（允许分隔符周围有空格）时，切换到
  正式图会把它转换为同 UUID 的 `TouchIdle`：框名取左编号、过渡动画取右编号。
  符合规则的两段在计划图中分别用蓝色和橙色显示。
- 不符合规则的主题保持为 `PlanPlaceholder`：可保留坐标、分组和所有入/出边，
  但不参与 CSV、正式业务校验或功能节点导出。回到计划图修改标题后，下次切换
  正式图会重新识别；一批主题的转换只产生一个撤销步骤。

## 5. 分组

多选至少两个节点后按 `Ctrl+G` 创建组框。

- 双击组标题可在画布上直接就地重命名，无需再经过对话框。
- 节点与组框重叠达到约四分之一即可拖入，拖出后组框不会自动收缩。
- 空组会保留，因此节点可以再次拖回。
- 同一节点只能属于一个组；重新打组会自动解除旧归属，不会形成嵌套组。
- 移动与入组/出组属于同一次撤销操作。
- 组框不会自动收缩；需要重新包住当前成员时，右键组框选择“适配内容”。

## 6. 画布参考图

可以用两种方式添加截图：

- 将截图复制到系统剪贴板，在画布上按 `Ctrl+V`。
- 使用“工具 → 添加参考图…”选择 PNG、JPG、BMP 或 WebP 文件。

参考图位于节点和连线下方，粘贴后会自动选中。拖动图片可移动位置，拖动右下角的金色手柄可分别调整宽度和高度；图片未选中时也会保留外框描边。移动和缩放均可撤销/重做，保存并重新打开 JSON 后尺寸保持不变。图片会先按最长边 4096 像素、总像素 1600 万的上限缩放，再转为 PNG，以 Base64 形式内嵌在 JSON 的 `canvas_images` 中；单个文件最多保存 32 张参考图，并限制单图与总数据量。移动配置文件时不会丢图，且参考图不会进入 CSV。

剪贴板优先级为：正在编辑的文本 → 节点专用数据 → 画布图片。

## 7. 保存、迁移和 CSV

- `Ctrl+S` 保存当前 JSON。
- `Ctrl+Z` / `Ctrl+Y` 撤销和重做。
- `Ctrl+C` / `Ctrl+V` / `Ctrl+D` 创建副本时，功能节点槽位、自动字段及手动字段末尾的数字会递增，并跳过已有冲突。
- 自定义“框”“参数”等字段会记录为手动覆盖值，保存重开后不会再被自动规则覆盖。
- 节点摘要中的框、过渡动画和参数在文本溢出时统一从左侧省略并右对齐，优先
  保留末尾编号；短文本仍保持居中。
- 参数专用表格中尚未按回车的输入，也会在保存、自动保存、切换文件或关闭前提交。
- “批量创建配置底座”已经替代旧的“从 CSV 创建模板”；CSV 预览仍然保留。
- “导出到 CSV”不再弹出多文件选择，直接导出当前内存图表，不会自动保存 JSON、
  清除未保存标记或覆盖旧 CSV。文件名格式为
  `ship_l2d_export_<当前JSON名>_<YYYYMMDD_HHMMSS>.csv`；未保存图表依次使用
  角色名和“未命名图表”，同秒重复导出追加 `_2`、`_3`。

## 8. 主题、搜索与 SVN

- 在“视图 → 外观”中切换白天/夜间模式，选择会自动保存。
- `Ctrl+F` 打开画布上方的弹出式搜索框；结果出现时不会挤压或遮挡画布布局，点击结果后自动关闭。
- 点击工具栏“提交当前 JSON 到 SVN”后，编辑器会先保存当前 JSON，再查找已保存路径、系统 PATH 或常见安装目录中的 SVN CLI。首次找不到时可手动选择 `svn.exe`。
- 未纳入版本控制的 JSON 会自动执行 `svn add --parents`；提交 message 默认为包含扩展名的 JSON 文件名。检查、添加、提交以及 CLI 输出都会显示在进度窗口中。
- “SVN 图表 Diff…”只读取当前 JSON 的 SVN 文件历史。默认比较最新提交与打开
  Diff 窗口时深拷贝的当前编辑内容；若两者一致，会自动改为最近两次文件提交。
  两侧也可任选提交版本或“当前编辑内容”，每次继续加载 100 条历史。
- Diff 是节点/字段/位置/尺寸、连线、分组、计划层级和顺序、正式/计划画笔及
  参考图片摘要的结构化比较。它通过 `svn info --xml`、`svn log --xml` 和
  `svn cat` 在内存中完成，不保存本地 Diff 快照，也不提供恢复、合并或覆盖。
  SVN CLI 与凭据缓存由用户已有客户端提供；编辑器不收集密码或自动信任证书。

## 9. AI 对话与 Tool API

- 右侧可折叠“AI 对话”面板连接 OpenAI-compatible
  `/v1/chat/completions`。设置只保存 Base URL、模型名和 API Key 环境变量名；
  默认读取 `OPENAI_API_KEY`，密钥本身不写入设置、日志或历史。
- 查询、搜索和校验工具自动执行。每轮节点增改删或文件写入会先显示结构化预览，
  只有确认后才作为一次撤销事务提交；拒绝时模型收到 `USER_REJECTED`。
- 对话历史按规范化图表路径保存在本机应用数据目录，支持清除和容量上限，不会
  写入业务 JSON/CSV。
- `EditorToolService.tool_definitions()` 与 `invoke_tool()` 提供稳定的用户级工具
  注册表。写入使用 UUID、批次内 `client_id` 和单调 `revision`，任一操作失败
  时整个批次不改变文档。
- `apply_graph_edits.operations` 已支持最多 500 项的批量 `update_node`、
  `add_node`、`delete_node`、`add_connection` 和 `delete_connection`。同一批次只
  确认一次、原子提交，并作为一个步骤撤销；失败会整体回滚。
- 模型响应、流式正文、工具参数和工具结果不设额外字节/字符上限。仍保留网络
  超时、取消、6 轮工具循环、每轮 32 个调用和 500 个图操作限制；对话历史磁盘
  配额只淘汰旧轮次，不会中断或隐藏当前大响应。

## 10. 局域网更新

- 客户端会自动发现同一局域网内正在运行且已经发布版本的更新主机，无需填写
  地址或端口。“更新设置…”只用于配置自动发现失败后的手动回退地址。
- 发布电脑在“工具 → 局域网更新主机…”中唤醒独立托盘 Host，导入
  `.l2dupdate` 并启动服务；关闭编辑器或 Host 窗口都不会停止服务。托盘提供
  “显示”“启动/停止服务”“复制地址”“退出”；只有“退出”会结束 Host。
  首次
  对其他电脑提供服务时，按需点击“创建局域网防火墙规则…”。
- 1.1.0 及更高版本的发布电脑可以更新自己：保持 Host 运行，在同一个
  编辑器中点击“检查更新…”，程序会探测 `127.0.0.1`。1.0.0 首次升级到
  1.1.0 时还没有自动发现功能，需要直接运行 Setup，或复制 Host 窗口显示的
  地址；同机可按窗口端口手动构造 `http://127.0.0.1:<HTTP 端口>` 作为回退。
  下载安装包并完成签名与 SHA-256 校验后，确认安装会通过 IPC 优雅停止 Host；
  安装器从缓存/临时目录完成双程序更新，成功后重启编辑器并恢复升级前运行的 Host。
- 自动发现只传递候选地址，不能替代签名校验。发现端口被其他程序占用时，
  HTTP 服务仍会运行，可复制窗口中的地址并填入手动回退设置。

## 11. 测试与启动

从仓库根目录启动：

```bash
python -m l2d_config_editor.main
```

运行全部测试：

```bash
python -m unittest discover -s tests -v
```
