# L2D Config Editor 使用说明

## 1. 推荐工作流

1. 首次启动时选择保存 JSON 配置的工作目录；工具会记住该目录。之后可通过“文件 → 更改工作区…”切换。
2. 点击“创建配置底座”，在每一行填写角色名、角色资源名、角色 ID、版本、作者和允许目光拖拽的待机；作者可留空。
3. 需要一次创建多名角色时，在表格里添加多行；也可以从 Excel 复制后点击“粘贴多行”。版本、作者和目光拖拽待机可随角色分别变化。
4. 工具会按版本建立目录，为每行生成一份 JSON，并打开第一份配置。
5. 从画布上的 `idle0（默认待机）` 输出点开始，创建并连接后续交互。
6. 完成后按 `Ctrl+S` 保存；需要检查最终表格时使用 CSV 预览或导出。若当前目录属于 SVN 工作副本，也可点击“提交当前 JSON 到 SVN”。

角色元数据保存在 JSON 顶层的隐式 `meta` 中，不再显示为常驻的“初始节点”。旧版配置会在打开时自动迁移，原 Initial 的位置和出边会保留给 idle0。

## 2. 节点类型

- `idle0（默认待机）`：每份配置唯一的根节点，不可复制、删除或作为连线目标。
- `TouchIdle`：点击或待机切换类交互。
- `TouchDrag`：拖拽或数值变化类交互。
- `参数专用节点`：以表格形式批量维护参数触发行。
- `返回默认待机`：用法与 TouchIdle 相近，但目标待机固定为 idle0，无需填写。
- `备注节点`：只显示一个多行大标题；双击标题直接编辑，`Ctrl+Enter` 或失焦提交，不参与 CSV 输出。

双击功能节点可在摘要卡片和完整字段表单之间切换。旧的全局“简易/高级”入口已经移除；如以后需要不同字段层级，应在节点自身实现。

## 3. 画布操作

- 滚轮：缩放画布（具体修饰键可在快捷键设置中调整）。
- 鼠标中键拖动：平移画布。
- 右键空白处：创建节点。
- 从输出点拖到输入点：建立连接。
- 从输出点拖到空白处：创建新节点并自动连接。
- `Esc`：取消正在创建的连接或关闭弹出式搜索框。
- `Delete`：删除选中的节点、连线或参考图。
- `F`：定位当前节点。
- `Ctrl+L`：优化已连接节点的布局。

所有正式连接均使用贝塞尔曲线。选中节点或连线后，方向箭头、脉冲和尾迹会沿曲线运动；大量节点下会自动降低动效密度，但不会退化成直线。

## 4. 分组

多选至少两个节点后按 `Ctrl+G` 创建组框。

- 双击组标题可在画布上直接就地重命名，无需再经过对话框。
- 节点与组框重叠达到约四分之一即可拖入，拖出后组框不会自动收缩。
- 空组会保留，因此节点可以再次拖回。
- 同一节点只能属于一个组；重新打组会自动解除旧归属，不会形成嵌套组。
- 移动与入组/出组属于同一次撤销操作。
- 组框不会自动收缩；需要重新包住当前成员时，右键组框选择“适配内容”。

## 5. 画布参考图

可以用两种方式添加截图：

- 将截图复制到系统剪贴板，在画布上按 `Ctrl+V`。
- 使用“工具 → 添加参考图…”选择 PNG、JPG、BMP 或 WebP 文件。

参考图位于节点和连线下方，可选择、拖动、删除并撤销。图片会先按最长边 4096 像素、总像素 1600 万的上限缩放，再转为 PNG，以 Base64 形式内嵌在 JSON 的 `canvas_images` 中；单个文件最多保存 32 张参考图，并限制单图与总数据量。移动配置文件时不会丢图，且参考图不会进入 CSV。

剪贴板优先级为：正在编辑的文本 → 节点专用数据 → 画布图片。

## 6. 保存、迁移和 CSV

- `Ctrl+S` 保存当前 JSON。
- `Ctrl+Z` / `Ctrl+Y` 撤销和重做。
- `Ctrl+C` / `Ctrl+V` / `Ctrl+D` 创建副本时，功能节点槽位、自动字段及手动字段末尾的数字会递增，并跳过已有冲突。
- 自定义“框”“参数”等字段会记录为手动覆盖值，保存重开后不会再被自动规则覆盖。
- 参数专用表格中尚未按回车的输入，也会在保存、自动保存、切换文件或关闭前提交。
- “批量创建配置底座”已经替代旧的“从 CSV 创建模板”；CSV 预览和 CSV 导出仍然保留。

## 7. 主题、搜索与 SVN

- 在“视图 → 外观”中切换白天/夜间模式，选择会自动保存。
- `Ctrl+F` 打开画布上方的弹出式搜索框；结果出现时不会挤压或遮挡画布布局，点击结果后自动关闭。
- 点击工具栏“提交当前 JSON 到 SVN”后，编辑器会先保存当前 JSON，再查找已保存路径、系统 PATH 或常见安装目录中的 SVN CLI。首次找不到时可手动选择 `svn.exe`。
- 未纳入版本控制的 JSON 会自动执行 `svn add --parents`；提交 message 默认为包含扩展名的 JSON 文件名。检查、添加、提交以及 CLI 输出都会显示在进度窗口中。

## 8. 测试与启动

从仓库根目录启动：

```bash
python -m l2d_config_editor.main
```

运行全部测试：

```bash
python -m unittest discover -s tests -v
```
