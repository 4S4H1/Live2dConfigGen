# 2026-09-19 工程审查与修复

本轮检查覆盖数据模型、JSON 读写与迁移、CSV 导出、自动规则、计划图、监听器、
编辑与撤销、画布及参考图、工作区文件操作、SVN 历史、AI 工具调用、更新签名与下载、
单实例通信、安装文件管理和 CI。对确认的问题先建立失败回归，再修复并执行相关测试。

## 已修复问题

| 范围 | 原问题 | 修复后行为 |
| --- | --- | --- |
| 字段编辑与撤销 | 撤销遗留手工覆盖标记，无法恢复联动生成字段 | 保存编辑前后的字段、覆盖标记和计划标题，撤销/重做完整恢复 |
| 文件打开与删除撤销 | 打开损坏 JSON 先清空撤销栈；恢复删除对象改变顺序 | 成功解析后才切换文档；节点、连线、参考图恢复原次序 |
| 文档与节点快照 | 嵌套字段、坐标和旧 history 与调用者共享引用 | 克隆、基于节点创建、载入和导出结果相互隔离 |
| 数值精度与校验 | parts_data 被截为六位有效数字；非有限值和科学记数范围漏检 | 保留浮点可往返精度，拒绝非有限值，正确解析范围 |
| 批量模板创建 | 发布覆盖并发出现的同名文件；粘贴空列丢失、数据误作表头 | 原子发布拒绝覆盖，处理 Windows 文件名，保留空列并收紧表头识别 |
| 工作区重命名 | 文件名可含路径、冲突抛异常、扩展名重复、缓存路径失配 | 校验名称、提示冲突、正确处理扩展名并迁移缓存与撤销栈；保留目录名中的点 |
| SVN 查询 | 旧取消定时器可能杀掉后续新查询 | 查询完成或重新开始时取消旧定时器 |
| AI 确认与对话 | 取消后仍可能执行变更；同图重发可恢复旧确认；磁盘/异常回复中断对话 | 按独立请求代号校验确认生命周期；历史写盘失败保留内存对话；异常工具数据按错误处理 |
| AI 图表工具 | 小数静默截断为整数；布局允许越界甚至无穷坐标 | 严格校验整数及最终布局坐标，失败不修改文档或撤销栈 |
| 更新下载 | 缓存 I/O 异常逃出 Qt 回调；锁定旧缓存阻断成功下载 | 释放下载资源并发出错误；清理旧缓存失败不影响已验证新包 |
| 更新 HTTP | If-Range 不匹配仍返回部分文件 | 不匹配时返回完整文件，避免混合不同版本内容 |
| 单实例通信 | 分片消息丢失、同步接收阻塞、非法 JSON 抛错、相对路径解析错误 | 事件驱动缓冲与超时，校验消息结构，发送前解析绝对路径 |
| 安装与卸载 | Windows PowerShell 按 ANSI 读取 UTF-8 清单 | 显式读取 UTF-8，中文受管资源与用户文件可正确区分 |
| CI | 使用旧 Python 3.11、未锁定环境和单进程测试入口 | 使用 Python 3.13.14、uv 锁文件及项目隔离测试入口 |

## 验证

最终全量运行 `python scripts/run_tests.py`：**40 个隔离模块、694 项测试全部通过，
无失败、无跳过**。本轮新增 42 项回归测试；各分区还分别执行了失败复现和定向验证。

测试环境：Windows、Python 3.13.14、PySide6 6.11.1、NSIS 3.12、SVN 1.14.5。
真实 SVN 集成测试通过在测试进程 PATH 中加入 `.tools/svn-1.14.5/bin` 启用。
安装器测试使用隔离的临时目录和测试标识。另执行 `uv lock --check`、Python 编译检查与
`git diff --check`，均通过；`uv pip check` 确认当前 16 个安装包兼容。

本轮提交包含源代码、回归测试和 CI 修正，不生成或发布签名安装包。
开始审查前已有的 `20260711/1.json`、`20260711/4.json` 修改和两份 CSV 导出保持在工作区，
不纳入本次修复提交。此次检查及测试不代表对所有运行环境与输入组合的绝对无缺陷保证。
